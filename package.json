{
  "name": "stable-tap",
  "version": "0.1.0",
  "description": "A simple browser extension for receiving stablecoin taproot assets",
  "type": "module",
  "main": "dist/background.js",
  "author": "Bryan Nonni (@bnonni)",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "start": "npm run build && node ./dist/index.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
